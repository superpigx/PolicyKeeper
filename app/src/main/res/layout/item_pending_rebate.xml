<?xml version="1.0" encoding="utf-8"?>
<!--
  待收返款清单里的一行。

  信息顺序按「识别 → 核对 → 动作」排：
  先给投保对象（最快的识别线索），再给险种与起保日期（用来区分同一对象的哪一年哪一张），
  然后是待收金额，最后才是标记按钮。金额与按钮都靠右对齐成一列，扫视时不会左右乱跳。
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="?attr/selectableItemBackground"
    android:clickable="true"
    android:focusable="true"
    android:gravity="center_vertical"
    android:orientation="horizontal"
    android:paddingTop="11dp"
    android:paddingBottom="11dp">

    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginEnd="10dp"
        android:layout_weight="1"
        android:orientation="vertical">

        <TextView
            android:id="@+id/tvInsured"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:ellipsize="end"
            android:includeFontPadding="false"
            android:maxLines="1"
            android:textColor="@color/text_primary"
            android:textSize="15sp"
            android:textStyle="bold"
            tools:text="京A·12345" />

        <TextView
            android:id="@+id/tvMeta"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="3dp"
            android:ellipsize="end"
            android:includeFontPadding="false"
            android:maxLines="1"
            android:textColor="@color/text_tertiary"
            android:textSize="12sp"
            tools:text="交强险 · 2026.03.01" />
    </LinearLayout>

    <TextView
        android:id="@+id/tvAmount"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginEnd="10dp"
        android:includeFontPadding="false"
        android:textColor="@color/status_warn"
        android:textSize="15sp"
        android:textStyle="bold"
        tools:text="¥150" />

    <com.google.android.material.button.MaterialButton
        android:id="@+id/btnMark"
        style="@style/Widget.PolicyKeeper.MiniButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/rebate_mark_done" />
</LinearLayout>
